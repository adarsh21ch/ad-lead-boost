# Supabase (new, isolated project — not nFlow's)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Meta App (created in developers.facebook.com — Business type app)
META_APP_ID=
META_APP_SECRET=
META_OAUTH_REDIRECT_URI=http://localhost:3000/auth/meta/callback

# Encryption key for storing account access tokens at rest (32-byte hex)
TOKEN_ENCRYPTION_KEY=
